version: 2
jobs:
  build:
    machine:
      java:
        version: openjdk7
    steps:
      - checkout
      - run: ant test
      - run: mkdir /tmp/wars
      - run: jar -cvf /tmp/wars/sample.war META-INF/ WEB-INF/ hello.jsp images/ index.html
  
  deploy:
    machine:
      java:
        version: openjdk7
    steps:
      - checkout
      - run: ant test
      
      - run: aws s3 sync /tmp/wars s3://build-artifacts-java-aig-test/java-apps/ --delete

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build     
